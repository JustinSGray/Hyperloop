To use these scripts to generate XDSM diagrams, you must run them all from **THIS** directory

The also rely on you having pdflatex installed on your machine. 

The scripts will generate a pdf file. You have to export that file to a png image and copy it 
to the appropriate file in docs/images/<filename>.png